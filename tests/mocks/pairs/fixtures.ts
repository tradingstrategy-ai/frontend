/**
 * Slug of a fixture pair with no TVL, liquidity or volume, used to test that
 * dead / spam pair pages are marked `noindex` (see `$lib/explorer/indexing`).
 */
export const ILLIQUID_PAIR_SLUG = 'dead-eth';
